name: stdnum_dart
description: A library for validating and formatting standard document numbers.
version: 1.0.0
repository: https://github.com/augustodia/stdnum-dart

environment:
  sdk: ^3.7.2

# Add regular dependencies here.
# dependencies:
#   # path: ^1.8.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
